import re
import json
# with open('b_all_links_z.txt', 'r', encoding='utf-8') as f:
#     lines = f.readlines()
# lines = [line.replace('\n', '') for line in lines]
# with open('')
# pattern = r"\((.*?)\)"

# for line in lines:
#     name = line.split('|||')[0]
#     matches = re.findall(pattern, name)
#     print(matches)

    # a = name.count('(')
    # if a > 1:
    #     print(line)